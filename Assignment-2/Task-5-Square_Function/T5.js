// Task 5: Function to calculate square of a number
function square(num) {
  return num * num;
}

// Test the function
let testNumber = 4;
let squareResult = square(testNumber);

// Display results
console.log("The square of", testNumber, "is:", squareResult);
console.log("The square of 9 is:", square(9));